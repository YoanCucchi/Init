# question is weird but if i understand correctly
for graphic session : boot the system and enter our username and password
for ssh : same as question 17, ssh user@hostname and enter password if asked
